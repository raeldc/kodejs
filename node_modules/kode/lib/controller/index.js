defineObject('lib:controller', 'lib:object',
{
    configure: function(config)
    {
        config.append({
            request  : 'lib:controller.request',
            response : 'lib:controller.response',
            behaviors: ['permissible'],
        });

        this.parentMethod(config);
    }
});