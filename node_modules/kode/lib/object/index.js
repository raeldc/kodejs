defineObject('lib:object',
{
    construct: function(config)
    {
        this.configure(config);
    },

    configure: function(config)
    {
        config.append({
            mixins    : {},
            decorators: {}
        });
    }
});